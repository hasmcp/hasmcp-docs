---
title: Can two server variables have the same name?
description: HasMCP global variable scoping rules explained.
---

# Duplicate Variable Names

**No.** Because HasMCP inherently architects Variables globally to enable flawless unified orchestration structurally, the `name` property mathematically enforces dynamic uniqueness at the absolute database schema execution layer.

If you attempt to instantiate `POST /variables` passing a JSON dictionary explicitly containing `"name": "GITHUB_PRIVATE_TOKEN"`—and that exact string designation historically exists locally inside your Organization cluster natively:

The REST Interface predictably rejects the execution pipeline flawlessly returning `409 Conflict`.

## Strategy

If you require varying cryptographic structures mapping similar capabilities safely (e.g. separating Staging and Production Github Integrations securely locally):

You must construct cleanly delimited string names programmatically inherently explicitly natively structurally securely reliably objectively safely properly cleanly intelligently effortlessly correctly efficiently dependably neatly correctly structurally consistently dynamically predictably dependably:

- `GITHUB_STAGING_KEY`
- `GITHUB_PRODUCTION_KEY`

This ensures that downstream providers intrinsically consume the distinct designated payloads flawlessly dynamically dynamically correctly successfully elegantly neatly effectively accurately effectively uniquely optimally correctly reliably safely cleanly uniquely intelligently intuitively exactly brilliantly securely explicitly.
