openapi: 3.0.4
info:
  title: HasMCP Manager API
  version: "1.0.0"
  description: API for managing HasMCP components like Variables, Providers, MCP Servers, Resources, and Prompts.
security:
  - bearerAuth: []
paths:
  /variables:
    get:
      summary: List Variables
      operationId: listVariables
      security:
        - oauth2:
            - "variable:list"
      responses:
        "200":
          description: A list of variables
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListVariablesResponse"
    post:
      summary: Create Variable
      operationId: createVariable
      security:
        - oauth2:
            - "variable:create"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVariableRequest"
      responses:
        "201":
          description: Variable created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateVariableResponse"
  /variables/{id}:
    patch:
      summary: Update Variable
      operationId: updateVariable
      security:
        - oauth2:
            - "variable:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Variable ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVariableRequest"
      responses:
        "200":
          description: Variable updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateVariableResponse"
    delete:
      summary: Delete Variable
      operationId: deleteVariable
      security:
        - oauth2:
            - "variable:delete"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Variable ID
      responses:
        "204":
          description: Variable deleted
  /providers:
    get:
      summary: List Providers
      operationId: listProviders
      security:
        - oauth2:
            - "provider:list"
      parameters:
        - name: nameContains
          in: query
          schema:
            type: string
        - name: baseURLContains
          in: query
          schema:
            type: string
        - name: apiType
          in: query
          schema:
            type: string
        - name: visibility
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
        - name: token
          in: query
          schema:
            type: string
      responses:
        "200":
          description: A list of providers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListProvidersResponse"
    post:
      summary: Create Provider
      operationId: createProvider
      security:
        - oauth2:
            - "provider:create"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProviderRequest"
      responses:
        "201":
          description: Provider created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProviderResponse"
  /providers/{id}:
    get:
      summary: Get Provider
      operationId: getProvider
      security:
        - oauth2:
            - "provider:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      responses:
        "200":
          description: Provider details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetProviderResponse"
    patch:
      summary: Update Provider
      operationId: updateProvider
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProviderRequest"
      responses:
        "200":
          description: Provider updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateProviderResponse"
    delete:
      summary: Delete Provider
      operationId: deleteProvider
      security:
        - oauth2:
            - "provider:delete"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      responses:
        "204":
          description: Provider deleted
  /providers/{id}/tools:
    get:
      summary: List Provider Tools
      operationId: listProviderTools
      security:
        - oauth2:
            - "provider:list"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      responses:
        "200":
          description: A list of provider tools
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListProviderToolsResponse"
    post:
      summary: Create Provider Tool
      operationId: createProviderTool
      security:
        - oauth2:
            - "provider:create"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProviderToolRequest"
      responses:
        "201":
          description: Tool created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProviderToolResponse"
  /providers/{id}/tools/{toolID}:
    get:
      summary: Get Provider Tool
      operationId: getProviderTool
      security:
        - oauth2:
            - "provider:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: toolID
          in: path
          required: true
          schema:
            type: string
            description: Tool ID
      responses:
        "200":
          description: Tool details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetProviderToolResponse"
    patch:
      summary: Update Provider Tool
      operationId: updateProviderTool
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: toolID
          in: path
          required: true
          schema:
            type: string
            description: Tool ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProviderToolRequest"
      responses:
        "200":
          description: Tool updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateProviderToolResponse"
    delete:
      summary: Delete Provider Tool
      operationId: deleteProviderTool
      security:
        - oauth2:
            - "provider:delete"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: toolID
          in: path
          required: true
          schema:
            type: string
            description: Tool ID
      responses:
        "204":
          description: Tool deleted
  /servers:
    get:
      summary: List MCP Servers
      operationId: listServers
      security:
        - oauth2:
            - "server:list"
      responses:
        "200":
          description: A list of MCP servers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServersResponse"
    post:
      summary: Create MCP Server
      operationId: createServer
      security:
        - oauth2:
            - "server:create"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServerRequest"
      responses:
        "201":
          description: MCP Server created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServerResponse"
  /servers/import:
    post:
      summary: Import MCP Server
      operationId: importServer
      security:
        - oauth2:
            - "server:update"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: ExportServer JSON schema spec
      responses:
        "201":
          description: MCP Server imported successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetServerResponse"
  /servers/{id}:
    get:
      summary: Get MCP Server
      operationId: getServer
      security:
        - oauth2:
            - "server:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      responses:
        "200":
          description: MCP Server details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetServerResponse"
    patch:
      summary: Update MCP Server
      operationId: updateServer
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateServerRequest"
      responses:
        "200":
          description: MCP Server updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateServerResponse"
    delete:
      summary: Delete MCP Server
      operationId: deleteServer
      security:
        - oauth2:
            - "server:delete"
      parameters:
        - name: id
          in: path
          required: true
          description: MCP Server ID
          schema:
            type: string
            description: Git Connection ID
      responses:
        "204":
          description: MCP Server deleted
  /servers/{id}/export:
    get:
      summary: Export MCP Server
      operationId: exportServer
      security:
        - oauth2:
            - "server:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      responses:
        "200":
          description: MCP Server exported successfully
          content:
            application/json:
              schema:
                type: object
                description: ExportServer JSON schema spec
  /servers/{id}/tokens:
    post:
      summary: Create MCP Server Token
      operationId: createServerToken
      security:
        - oauth2:
            - "server:create_token"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServerTokenRequest"
      responses:
        "201":
          description: MCP Server Token created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServerTokenResponse"
  /servers/{id}/tools:
    get:
      summary: List MCP Server Tools
      operationId: listServerTools
      security:
        - oauth2:
            - "server:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      responses:
        "200":
          description: A list of associated tools
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServerToolsResponse"
    post:
      summary: Create MCP Server Tool Association
      operationId: createServerTool
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServerToolRequest"
      responses:
        "201":
          description: Tool associated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServerToolResponse"
  /servers/{id}/tools/{toolID}:
    delete:
      summary: Delete MCP Server Tool Association
      operationId: deleteServerTool
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
        - name: toolID
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Tool association deleted
  /providers/{id}/resources:
    get:
      summary: List Provider Resources
      operationId: listProviderResources
      security:
        - oauth2:
            - "provider:list"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      responses:
        "200":
          description: A list of provider resources
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListProviderResourcesResponse"
    post:
      summary: Create Provider Resource
      operationId: createProviderResource
      security:
        - oauth2:
            - "provider:create"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProviderResourceRequest"
      responses:
        "201":
          description: Provider Resource created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProviderResourceResponse"
  /providers/{id}/resources/{resourceID}:
    get:
      summary: Get Provider Resource
      operationId: getProviderResource
      security:
        - oauth2:
            - "provider:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: resourceID
          in: path
          required: true
          schema:
            type: string
            description: Resource ID
      responses:
        "200":
          description: Provider Resource details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetProviderResourceResponse"
    patch:
      summary: Update Provider Resource
      operationId: updateProviderResource
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: resourceID
          in: path
          required: true
          schema:
            type: string
            description: Resource ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProviderResourceRequest"
      responses:
        "200":
          description: Provider Resource updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateProviderResourceResponse"
    delete:
      summary: Delete Provider Resource
      operationId: deleteProviderResource
      security:
        - oauth2:
            - "provider:delete"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: resourceID
          in: path
          required: true
          schema:
            type: string
            description: Resource ID
      responses:
        "204":
          description: Provider Resource deleted
  /providers/{id}/prompts:
    get:
      summary: List Provider Prompts
      operationId: listProviderPrompts
      security:
        - oauth2:
            - "provider:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      responses:
        "200":
          description: A list of provider prompts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListProviderPromptsResponse"
    post:
      summary: Create Provider Prompt
      operationId: createProviderPrompt
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProviderPromptRequest"
      responses:
        "201":
          description: Provider Prompt created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProviderPromptResponse"
  /providers/{id}/prompts/{promptID}:
    get:
      summary: Get Provider Prompt
      operationId: getProviderPrompt
      security:
        - oauth2:
            - "provider:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: promptID
          in: path
          required: true
          schema:
            type: string
            description: Prompt ID
      responses:
        "200":
          description: Provider Prompt details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetProviderPromptResponse"
    patch:
      summary: Update Provider Prompt
      operationId: updateProviderPrompt
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: promptID
          in: path
          required: true
          schema:
            type: string
            description: Prompt ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProviderPromptRequest"
      responses:
        "204":
          description: Provider Prompt updated
    delete:
      summary: Delete Provider Prompt
      operationId: deleteProviderPrompt
      security:
        - oauth2:
            - "provider:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Provider ID
        - name: promptID
          in: path
          required: true
          schema:
            type: string
            description: Prompt ID
      responses:
        "204":
          description: Provider Prompt deleted
  /servers/{id}/resources:
    get:
      summary: List MCP Server Resource Associations
      operationId: listServerResources
      security:
        - oauth2:
            - "server:list"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      responses:
        "200":
          description: A list of associated resources
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServerResourcesResponse"
    post:
      summary: Create MCP Server Resource Association
      operationId: createServerResource
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServerResourceRequest"
      responses:
        "201":
          description: Resource associated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServerResourceResponse"
  /servers/{id}/resources/{resourceID}:
    delete:
      summary: Delete MCP Server Resource Association
      operationId: deleteServerResource
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
        - name: resourceID
          in: path
          required: true
          schema:
            type: string
            description: Resource ID to disassociate
      responses:
        "204":
          description: Resource association deleted
  /servers/{id}/prompts:
    get:
      summary: List MCP Server Prompt Associations
      operationId: listServerPrompts
      security:
        - oauth2:
            - "server:read"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      responses:
        "200":
          description: A list of associated prompts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServerPromptsResponse"
    post:
      summary: Create MCP Server Prompt Association
      operationId: createServerPrompt
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServerPromptRequest"
      responses:
        "201":
          description: Prompt associated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateServerPromptResponse"
  /servers/{id}/prompts/{promptID}:
    delete:
      summary: Delete MCP Server Prompt Association
      operationId: deleteServerPrompt
      security:
        - oauth2:
            - "server:update"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: MCP Server ID
        - name: promptID
          in: path
          required: true
          schema:
            type: string
            description: Prompt ID to disassociate
      responses:
        "204":
          description: Prompt association deleted
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: "Enter 'Bearer' followed by a space and your token (e.g., 'Bearer ${HASMCP_ACCESS_TOKEN}')."
    oauth2:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://accounts.hasmcp.com/oauth2/auth
          tokenUrl: https://accounts.hasmcp.com/oauth2/token
          scopes:
            "gitconnection:list": List Git connections
            "gitconnection:delete": Delete Git connection
            "variable:list": List variables
            "variable:create": Create variable
            "variable:update": Update variable
            "variable:delete": Delete variable
            "provider:list": List providers
            "provider:create": Create provider
            "provider:read": Get provider
            "provider:update": Update provider
            "provider:delete": Delete provider
            "server:list": List MCP servers
            "server:create": Create MCP server
            "server:read": Get MCP server
            "server:update": Update MCP server
            "server:delete": Delete MCP server
            "server:create_token": Create MCP server token
  schemas:
    Variable:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
        type:
          type: string
          description: "0: INVALID, 1: ENV, 2: SECRET"
          example: "ENV"
        value:
          type: string
          description: The variable's value. Will be '***' if type is SECRET on retrieval.
        name:
          type: string
    VariableCreate:
      type: object
      properties:
        type:
          type: string
          description: "0: INVALID, 1: ENV, 2: SECRET"
          example: "ENV"
        value:
          type: string
          description: The variable's value.
        name:
          type: string
    VariableUpdate:
      type: object
      properties:
        value:
          type: string
          description: The variable's value.
    CreateVariableRequest:
      type: object
      properties:
        variable:
          $ref: "#/components/schemas/VariableCreate"
    CreateVariableResponse:
      type: object
      properties:
        variable:
          $ref: "#/components/schemas/Variable"
    UpdateVariableRequest:
      type: object
      properties:
        variable:
          $ref: "#/components/schemas/VariableUpdate"
    UpdateVariableResponse:
      type: object
      properties:
        variable:
          $ref: "#/components/schemas/Variable"
    ListVariablesResponse:
      type: object
      properties:
        variables:
          type: array
          items:
            $ref: "#/components/schemas/Variable"
    ProviderOAuth2Config:
      type: object
      properties:
        clientID:
          type: string
        clientSecret:
          type: string
        authURL:
          type: string
          format: uri
        tokenURL:
          type: string
          format: uri
    Provider:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
        version:
          type: integer
          format: int32
          readOnly: true
        apiType:
          type: string
          description: "0: INVALID, 1: REST"
          example: "REST"
        visibilityType:
          type: string
          description: "0: INVALID, 1: INTERNAL, 2: PUBLIC"
          example: "PUBLIC"
        baseURL:
          type: string
          format: uri
        documentURL:
          type: string
          format: uri
        iconURL:
          type: string
          format: uri
        secretPrefix:
          type: string
          readOnly: true
        name:
          type: string
        description:
          type: string
        tools:
          type: array
          readOnly: true
          items:
            $ref: "#/components/schemas/ProviderTool"
        oauth2Config:
          $ref: "#/components/schemas/ProviderOAuth2Config"
    ProviderCreate:
      type: object
      properties:
        apiType:
          type: string
          description: "0: INVALID, 1: REST"
          example: "REST"
        visibilityType:
          type: string
          description: "0: INVALID, 1: INTERNAL, 2: PUBLIC"
          example: "PUBLIC"
        baseURL:
          type: string
          format: uri
        documentURL:
          type: string
          format: uri
        iconURL:
          type: string
          format: uri
        name:
          type: string
        description:
          type: string
        oauth2Config:
          $ref: "#/components/schemas/ProviderOAuth2Config"
    CreateProviderRequest:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/ProviderCreate"
    CreateProviderResponse:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/Provider"
    UpdateProviderRequest:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/ProviderCreate"
    UpdateProviderResponse:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/Provider"
    ListProvidersResponse:
      type: object
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/Provider"
        matchCount:
          type: integer
        nextToken:
          type: string
    GetProviderResponse:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/Provider"
    ProviderTool:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        providerID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        method:
          type: string
          example: "GET"
        path:
          type: string
          example: "/users/{id}"
        name:
          type: string
          description: MCP tool name (camelCase up to 20chars)
          example: makePayment
        title:
          type: string
          description: MCP tool title (Free text up to 64 chars)
          example: Make Payment
        description:
          type: string
        pathArgsJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        queryArgsJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        reqBodyJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        headers:
          type: array
          items:
            $ref: "#/components/schemas/ToolHeader"
        oauth2Scopes:
          type: array
          items:
            type: string
    ProviderToolCreate:
      type: object
      properties:
        providerID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          description: This will be ignored, as providerID is taken from the path.
        method:
          type: string
          example: "GET"
        path:
          type: string
          example: "/users/{id}"
        name:
          type: string
          description: MCP tool name (camelCase up to 20chars)
          example: makePayment
        title:
          type: string
          description: MCP tool title (Free text up to 64 chars)
          example: Make Payment
        description:
          type: string
        pathArgsJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        queryArgsJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        reqBodyJSONSchema:
          type: object
          description: A raw JSON schema.
          additionalProperties: true
        headers:
          type: array
          items:
            $ref: "#/components/schemas/ToolHeader"
        oauth2Scopes:
          type: array
          items:
            type: string
    ToolHeader:
      type: object
      properties:
        key:
          type: string
        value:
          type: string
    CreateProviderToolRequest:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ProviderToolCreate"
    CreateProviderToolResponse:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ProviderTool"
    UpdateProviderToolRequest:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ProviderToolCreate"
    UpdateProviderToolResponse:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ProviderTool"
    ListProviderToolsResponse:
      type: object
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/ProviderTool"
    GetProviderToolResponse:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ProviderTool"
    Server:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
        name:
          type: string
        description:
          type: string
        requestHeadersProxyEnabled:
          type: boolean
        dynamicToolDiscoveryEnabled:
          type: boolean
        version:
          type: integer
          format: int32
        gitProvider:
          type: string
        gitRepoFullName:
          type: string
        providers:
          type: array
          items:
            $ref: "#/components/schemas/Provider"
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ProviderResource"
        prompts:
          type: array
          items:
            $ref: "#/components/schemas/ProviderPrompt"
    ServerCreate:
      type: object
      properties:
        name:
          type: string
        instructions:
          type: string
        version:
          type: integer
          format: int32
        providers:
          type: array
          description: Array of providers to associate, only ID and nested Tool IDs are used.
          items:
            type: object
            properties:
              id:
                type: string
                pattern: "^[A-Za-z0-9]{11}$"
                example: "kSuB9Gf6aD4"
              tools:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      pattern: "^[A-Za-z0-9]{11}$"
                      example: "kSuB9Gf6aD4"
        resources:
          type: array
          description: Array of resources to associate, only ID is used.
          items:
            type: object
            properties:
              id:
                type: string
                pattern: "^[A-Za-z0-9]{11}$"
                example: "kSuB9Gf6aD4"
        prompts:
          type: array
          description: Array of prompts to associate, only ID is used.
          items:
            type: object
            properties:
              id:
                type: string
                pattern: "^[A-Za-z0-9]{11}$"
                example: "kSuB9Gf6aD4"
    CreateServerRequest:
      type: object
      properties:
        server:
          $ref: "#/components/schemas/ServerCreate"
    CreateServerResponse:
      type: object
      properties:
        server:
          $ref: "#/components/schemas/Server"
    ListServersResponse:
      type: object
      properties:
        servers:
          type: array
          items:
            $ref: "#/components/schemas/Server"
    GetServerResponse:
      type: object
      properties:
        server:
          $ref: "#/components/schemas/Server"
    UpdateServerRequest:
      type: object
      properties:
        server:
          $ref: "#/components/schemas/ServerCreate"
    UpdateServerResponse:
      type: object
      properties:
        server:
          $ref: "#/components/schemas/Server"
    ServerToken:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        serverID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        createdAt:
          type: string
          format: date-time
          readOnly: true
        expiresAt:
          type: string
          format: date-time
        name:
          type: string
        value:
          type: string
          description: Token value, only present on creation.
          readOnly: true
    ServerTokenCreate:
      type: object
      properties:
        expiresAt:
          type: string
          format: date-time
        name:
          type: string
    CreateServerTokenRequest:
      type: object
      properties:
        token:
          $ref: "#/components/schemas/ServerTokenCreate"
    CreateServerTokenResponse:
      type: object
      properties:
        token:
          $ref: "#/components/schemas/ServerToken"
    ServerTool:
      type: object
      properties:
        serverID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        providerID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        toolID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
    CreateServerToolRequest:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ServerTool"
    CreateServerToolResponse:
      type: object
      properties:
        tool:
          $ref: "#/components/schemas/ServerTool"
    ListServerToolsResponse:
      type: object
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/ServerTool"
    ProviderResource:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
        name:
          type: string
        description:
          type: string
        uri:
          type: string
          format: uri
        mimeType:
          type: string
          example: "text/plain"
        size:
          type: integer
          format: int64
        annotations:
          type: object
          description: A raw JSON message for annotations.
          additionalProperties: true
    ProviderResourceCreate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        uri:
          type: string
          format: uri
        mimeType:
          type: string
          example: "text/plain"
        size:
          type: integer
          format: int64
        annotations:
          type: object
          description: A raw JSON message for annotations.
          additionalProperties: true
    CreateProviderResourceRequest:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ProviderResourceCreate"
    CreateProviderResourceResponse:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ProviderResource"
    GetProviderResourceResponse:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ProviderResource"
    ListProviderResourcesResponse:
      type: object
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ProviderResource"
    UpdateProviderResourceRequest:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ProviderResourceCreate"
    UpdateProviderResourceResponse:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ProviderResource"
    ProviderPrompt:
      type: object
      properties:
        id:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
        name:
          type: string
        description:
          type: string
        arguments:
          type: object
          description: A raw JSON message for arguments (e.g., []PromptArgument).
          additionalProperties: true
        messages:
          type: object
          description: A raw JSON message for messages (e.g., []PromptMessage).
          additionalProperties: true
    ProviderPromptCreate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        arguments:
          type: object
          description: A raw JSON message for arguments (e.g., []PromptArgument).
          additionalProperties: true
        messages:
          type: object
          description: A raw JSON message for messages (e.g., []PromptMessage).
          additionalProperties: true
    CreateProviderPromptRequest:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ProviderPromptCreate"
    CreateProviderPromptResponse:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ProviderPrompt"
    GetProviderPromptResponse:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ProviderPrompt"
    ListProviderPromptsResponse:
      type: object
      properties:
        prompts:
          type: array
          items:
            $ref: "#/components/schemas/ProviderPrompt"
    UpdateProviderPromptRequest:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ProviderPromptCreate"
    ServerResource:
      type: object
      properties:
        serverID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        resourceID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
    CreateServerResourceRequest:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ServerResource"
    CreateServerResourceResponse:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ServerResource"
    ListServerResourcesResponse:
      type: object
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ServerResource"
    ServerPrompt:
      type: object
      properties:
        serverID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
        promptID:
          type: string
          pattern: "^[A-Za-z0-9]{11}$"
          example: "kSuB9Gf6aD4"
    CreateServerPromptRequest:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ServerPrompt"
    CreateServerPromptResponse:
      type: object
      properties:
        prompt:
          $ref: "#/components/schemas/ServerPrompt"
    ListServerPromptsResponse:
      type: object
      properties:
        prompts:
          type: array
          items:
            $ref: "#/components/schemas/ServerPrompt"
